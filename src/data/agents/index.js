import { heartAgent } from './heart_agent';
import { hospitalAgent } from './hospital_agent';
import { generalAgent } from './general_agent';
import { emergencyAgent } from './emergency_agent';

export const agents = [
  heartAgent,
  hospitalAgent,
  generalAgent,
  emergencyAgent
];

export {
  heartAgent,
  hospitalAgent,
  generalAgent,
  emergencyAgent
};
